## Pending
- [ ] Test Raw Input API di Windows — build, push, run `debug.bat`
- [ ] Add tray icon atau minimize-to-tray

## Done
- [x] Replace `GetAsyncKeyState` polling with Raw Input API (`RegisterRawInputDevices` + `WM_INPUT`)
  - [x] Mouse click detection via `RAWMOUSE.usButtonFlags`
  - [x] Keyboard toggle key detection via `RAWKEYBOARD.VKey`
  - [x] Hidden `HWND_MESSAGE` window + message pump
  - [x] No new dependencies — manual FFI to user32.dll
